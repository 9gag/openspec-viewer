# Publishing target. `publishConfig.registry` in package.json covers `npm publish`;
# this covers the `@seankcw` scope for anything that reads .npmrc instead, and is what
# the release workflow appends a token to.
@seankcw:registry=https://npm.pkg.github.com
